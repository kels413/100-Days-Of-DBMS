-- Using WHERE vs. ON:
--Explain the difference between applying a filter condition in the WHERE clause and in the ON clause when 
--using inner joins.

--Answer
--WHERE clause: Filters are applied after the join, so it filters the final result set.
--ON clause: Filters are applied during the join, affecting which rows are joined together.
